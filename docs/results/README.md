# Results

Store evaluation notes, latency breakdowns, ablation summaries, and links to generated plots here.

Large generated files should stay out of Git and live under ignored output directories such as `results/`, `outputs/`, or `artifacts/`.
